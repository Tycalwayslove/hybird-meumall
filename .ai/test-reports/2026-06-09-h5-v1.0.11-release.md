# 2026-06-09 H5 v1.0.11 发布验证记录

## 发布信息

- 发布版本：`v1.0.11`
- package version：`1.0.11`
- Git tag：`h5/v1.0.11`
- Git commit：`8d9073ae447e73aea3f4f01a4e6b091709d00360`
- Docker image：`meu-mall/h5:v1.0.11`
- Container：`meu-mall-h5-v1.0.11`
- Base path：`/h5-v/v1.0.11`
- Rollback version：`v1.0.10`
- Release ID：`21f40cdf-6e6e-4a96-a261-c0a9e9a6d3e5`

## 发布前验证

```bash
pnpm test
pnpm typecheck
pnpm lint
H5_BASE_PATH=/h5-v/v1.0.11 NEXT_PUBLIC_H5_BASE_PATH=/h5-v/v1.0.11 H5_RELEASE_LABEL=v1.0.11 H5_RELEASE_VARIANT=green pnpm build
```

结果：

- `pnpm test`：通过，27 files / 133 tests。
- `pnpm typecheck`：通过。
- `pnpm lint`：通过，存在 4 条历史 `<img>` warning，无 error。
- `pnpm build`：通过，生成路由表已不包含 `/member`。

## 线上发布

```bash
SERVER_PASSWORD=*** PROMOTE_RELEASE=true REGISTER_RELEASE=true RUN_REMOTE_SMOKE=true INSTALL_NGINX=true pnpm run deploy:h5-version
```

结果：

- 远端 Docker build 通过。
- H5 version container 启动：`127.0.0.1:3211`。
- nginx 版本入口安装并 reload 成功。
- remote smoke 通过。
- release 注册为 candidate 后 promote 为 active。

## 公网复核

```bash
curl -fsS 'https://hybird.aigcpop.com/api/h5/manifest/active?environment=prod'
curl -k -sS -o /tmp/h5-smoke.html -w '%{http_code}' 'https://hybird.aigcpop.com/h5-v/v1.0.11/api/health'
curl -k -sS -o /tmp/h5-smoke.html -w '%{http_code}' 'https://hybird.aigcpop.com/h5-v/v1.0.11/promotion'
curl -k -sS -o /tmp/h5-smoke.html -w '%{http_code}' 'https://hybird.aigcpop.com/h5-v/v1.0.11/mine'
curl -k -sS -o /tmp/h5-smoke.html -w '%{http_code}' 'https://hybird.aigcpop.com/h5-v/v1.0.11/search'
curl -k -sS -o /tmp/h5-smoke.html -w '%{http_code}' 'https://hybird.aigcpop.com/h5-v/v1.0.11/member'
```

结果：

- active manifest：`stableVersion=v1.0.11`，`rollbackVersion=v1.0.10`。
- `/h5-v/v1.0.11/api/health`：200。
- `/h5-v/v1.0.11/promotion`：200。
- `/h5-v/v1.0.11/mine`：200。
- `/h5-v/v1.0.11/search`：200。
- `/h5-v/v1.0.11/member`：404，符合删除旧 member 入口的预期。

## 风险和回滚

- 原生 iOS / Android 的正式 Bridge 容器逻辑仍由原生同学实现，本次只发布 H5。
- 如需回滚，server-meumall active manifest 可切回 `v1.0.10`。
